// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Post {
  id            String   @id @default(cuid())
  title         String
  slug          String   @unique
  content       String
  published     Boolean  @default(false)
  coverImageUrl String?
  // Focal point for the cover image (percentages, 0–100), used as CSS
  // object-position wherever the cover is shown cover-cropped: the post's
  // header and the home hero. 50/50 is centered (the default).
  coverFocalX   Int      @default(50)
  coverFocalY   Int      @default(50)
  // When true, this post appears as a slide in the home hero (cover image +
  // title linking to the post).
  featured      Boolean  @default(false)
  photos        Photo[]
  createdAt     DateTime @default(now())
}

// The editable "about" page. A single row (id is always "about"); the body is
// plain text where blank lines separate paragraphs.
model AboutPage {
  id          String   @id @default("about")
  body        String
  portraitUrl String?
  email       String?
  updatedAt   DateTime @updatedAt
}

// A message sent through the contact form on the about page. Kept in an admin
// inbox so Olha's email address never appears on the public site.
model ContactMessage {
  id        String   @id @default(cuid())
  name      String
  email     String
  subject   String?
  message   String
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}

model Photo {
  id        String   @id @default(cuid())
  url       String
  alt       String?
  caption   String?
  order     Int      @default(0)
  // Category for standalone gallery photos (null for post gallery photos).
  category  String?
  // When true, this gallery photo appears in the home hero carousel.
  featured  Boolean  @default(false)
  // Optional film stock the photo was shot on (e.g. "Kodak Portra 400").
  film      String?
  // Focal point for cropping, as percentages (0–100). Drives CSS
  // object-position when the photo is shown cover-cropped (e.g. the home hero),
  // so the important part stays in frame. 50/50 is dead-center (the default).
  focalX    Int      @default(50)
  focalY    Int      @default(50)
  // A photo belongs to a post (the per-post gallery). Deleting the post
  // removes its photo rows too. Standalone gallery photos have no post.
  post      Post?    @relation(fields: [postId], references: [id], onDelete: Cascade)
  postId    String?
  createdAt DateTime @default(now())
}
